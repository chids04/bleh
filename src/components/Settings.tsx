import { useEffect, useState } from "react"
import { Button } from "@/components/ui/button"

export default function HomePage() {

    const handleClick = async () => {

    }

    return (
        <div>
            <Button onClick={handleClick}>scan directory</Button>
        </div>
    )
}
